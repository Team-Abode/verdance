package com.teamabode.verdance.core.integration.farmersdelight;

import com.nhoryzon.mc.farmersdelight.FarmersDelightMod;
import com.nhoryzon.mc.farmersdelight.registry.BlocksRegistry;
import com.teamabode.verdance.core.misc.VerdanceGroupEvents;
import com.teamabode.verdance.core.registry.VerdanceBlocks;
import net.fabricmc.fabric.api.registry.FlammableBlockRegistry;
import net.fabricmc.loader.api.FabricLoader;

public class VerdanceFD {

    /*
    public static void register() {
        if (FabricLoader.getInstance().isModLoaded("farmersdelight")) {
            VerdanceGroupEvents.appendItemsBefore(FarmersDelightMod.ITEM_GROUP, BlocksRegistry.CRIMSON_CABINET.get(), VerdanceBlocks.MULBERRY_CABINET);
            FlammableBlockRegistry.getDefaultInstance().add(VerdanceBlocks.MULBERRY_CABINET, 5, 20);
        }
    }

     */
}
