## TODO for 1.20.1 port:

* Without `minecraft:jukebox_song`, revert to `MusicDiscItem` and `minecraft:music_discs` item tag.
* Without the `minecraft:arthropod` entity type tag, revert to `EntityGroup`



* Fix datagen
* Look for a config library to replace the current one.
* 